node_modules
dist
.wrangler
.dev.vars
*.local
.DS_Store
npm-debug.log*

# Machine-local Claude Code settings.
.claude/settings.local.json

# Stray bare clone of this same repo, left over from the first upload attempt.
ENVELOPE01/
